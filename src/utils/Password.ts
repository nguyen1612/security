import {  } from "crypto";

export class Password {
    /*
    TODO: Implement one-way hash function for securing the password
    */
    static async hash(password: string) {
        return password;
    }
}